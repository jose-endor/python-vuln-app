# Public npmjs.org only — avoids proxy URLs in package-lock that require org auth
# (breaks SCA/CI in sandboxes: E401 on factory.endorlabs.com-style resolved fields).
registry=https://registry.npmjs.org/
